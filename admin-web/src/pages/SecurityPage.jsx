import { useAuth } from '../auth/context.js'
import { Icon } from '../components/Icons.jsx'
export function SecurityPage() { const { session } = useAuth(); return <><header className="page-head"><div><span>系统与安全</span><h1>当前管理会话</h1><p>A1 仅展示已落地的安全能力；管理员与审计查询将在后续页面开放。</p></div></header><section className="security-grid"><article><Icon name="user"/><div><span>管理员</span><strong>{session.displayName}</strong><small>{session.username}</small></div></article><article><Icon name="shield"/><div><span>当前角色</span><strong>{session.role}</strong><small>后端接口二次校验</small></div></article><article><Icon name="lock"/><div><span>会话边界</span><strong>独立管理 Token</strong><small>aud: diet-tracker-admin</small></div></article></section><div className="info-banner"><Icon name="alert"/><p><strong>生产安全提示</strong>首次管理员仅允许通过显式环境变量引导创建，迁移不会写入默认账号或弱密码。</p></div></> }
