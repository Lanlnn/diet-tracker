# 开发复盘与防复发清单

本文记录 M0–M9 开发过程中已经实际发生的问题、根因和固定处理规则。它不是聊天记录，而是 M10、发布准备和后台管理开发的开工检查表。M0–M10 的集中成果演示与复盘测试见 [`M0-M10-REVIEW-AND-TEST.md`](./M0-M10-REVIEW-AND-TEST.md)。

## 1. 2026-07-15 项目快照

- `master` 已按依赖顺序完成 M0–M9，阶段标签为 `m0-complete`–`m9-complete`。
- M7 通过 PR #9 合并；后端、MySQL 迁移、小程序和开发者工具检查全部通过。
- M8 从包含 M7 的最新 `master` 独立整理，只包含趋势阶段的功能、测试、验收和文档改动。
- M8 完成 7/30/90 天、少于 3 天状态的契约联调和开发者工具整页截图，问题面板为 0。
- M9 完成饮食日历、日期边界和跨页聚合对账；M10 分支正在进行发布候选验收。
- 工作区还有与小程序里程碑无关的后台管理文档和原型改动，提交时必须继续按路径隔离。

## 2. 已发生的问题与固定规则

### 2.1 阶段依赖没有先合并

**现象：** M7 尚未进入 `master` 时开始 M8，导致 M8 分支同时包含 M7 和 M8 提交。直接向 `master` 创建 M8 PR 会把两个阶段混在一起，无法独立验收、回滚和打标签。

**固定规则：**

1. 阶段代码可以在明确的堆叠分支上提前开发，但交付必须严格按 M7 → M8 顺序。
2. 前置阶段 PR 合并后，后续阶段先变基到最新 `master`，确认 `git log master..HEAD` 只剩当前阶段提交，再创建 PR。
3. `mN-complete` 只能从合并后的 `master` 创建；本地完成或 Draft PR 都不能打完成标签。

### 2.2 工作区并行改动容易混入提交

**现象：** 开发 M8 时工作区同时出现 `admin-ui-ux-pro-max/`、`docs/ADMIN-DEVELOPMENT.md` 和 README 改动。使用 `git add -A` 会把无关功能带入里程碑提交。

**固定规则：**

- 开工和提交前都运行 `git status --short --branch`。
- 只用明确路径暂存，暂存后检查 `git diff --cached --check` 和 `git diff --cached --stat`。
- 不删除、不回滚来源不明的文件；无法隔离时停止提交并先确认归属。

### 2.3 本机 Java 与项目基线不一致

**现象：** 项目和 CI 曾使用 Java 17，但本机实际只有其他主版本。macOS 的 `java_home -v` 还可能静默回退，导致 Maven 运行时与编译目标不一致，Mockito/Byte Buddy 插桩错误被误判为业务失败。

**固定规则：**

```bash
java -version
cd backend
JAVA_HOME=$(/usr/libexec/java_home -v 17 2>/dev/null)
"$JAVA_HOME/bin/java" -version 2>&1 | rg 'version "17[.]' || exit 1
JAVA_HOME="$JAVA_HOME" sh mvnw --batch-mode test
```

- 本机 Homebrew OpenJDK 17.0.19 与 CI Temurin 17 使用同一 Java 17 主版本作为交付基准。
- macOS 可能在没有目标版本时静默返回其他唯一可用 JDK，必须对实际版本做断言；Maven Wrapper 也会拒绝非 Java 17。
- 遇到 Mockito/Byte Buddy “unsupported Java”时先核对运行时，不先修改业务代码或放宽测试。
- 新开发机如果没有 JDK 17，先安装并设置 `JAVA_HOME`，不要依赖其他版本碰巧兼容。

### 2.4 CI 只列举了早期测试

**现象：** 仓库已有 9 个小程序测试文件，旧 CI 只运行登录、资料和食品搜索 3 个；M4–M8 新增的计算、首页、餐次、运动和趋势测试不会在 PR 自动执行。

**固定规则：** CI 使用 `node --test miniapp/tests/*.test.js` 自动发现全部测试。新增测试不得再维护手写文件白名单。

### 2.5 Node 语法通过不代表开发者工具能编译

**现象：** 动态内联样式如 `style="width: {{value}}%"` 能通过 Node 语法检查，但微信开发者工具 Nightly 的生成 CSS 检查会报 `semi-colon expected`。M8 验收时发现并修复了首页遗留的两处问题。

**固定规则：**

- 提交前必须在微信开发者工具执行一次“普通编译”，问题面板必须为 0。
- 动态进度或柱高优先映射为有限 CSS class；确需动态样式时必须先用当前目标基础库和开发者工具验证。
- Node 测试、JSON/JS 静态检查、开发者工具编译是三道不同检查，互不替代。

### 2.6 自动化、联调和视觉验收曾被混为一个“完成”

**现象：** M8 的接口和流程测试全部通过，错误态也在开发者工具检查过，但因为本地后端未运行，成功态真实数据整页没有完成截图对照。此时只能标记“待验收”，不能标记“已完成”。

**固定规则：** 阶段完成必须分别记录：

1. 自动化：后端、前端、静态检查、真实 MySQL 迁移 CI。
2. 联调：小程序通过真实请求拿到符合契约的数据，并与相关页面对账。
3. 视觉：成功、空、错误等适用状态在目标模拟器或真机检查，并保存截图。
4. 交付：PR、CI、合并、标签。

四类证据没有齐全时，状态只能是`进行中`或`待验收`。

### 2.7 文档状态和架构描述会随代码漂移

**现象：** 里程碑曾出现“已完成（待验收合并）”“开发完成，待……”等自由文本，和规定的四种状态不一致；`architecture.md` 仍描述旧四 Tab、旧统计页和“Java 版本待统一”。

**固定规则：**

- 状态字段只写`未开始`、`进行中`、`待验收`、`已完成`；日期和说明另起一段。
- 每次修改导航、核心实体、接口或运行时基线时，同时更新 `architecture.md`、`api.md` 和当前里程碑文档。
- 开始下一阶段前运行：

```bash
rg -n '^\*\*状态：\*\*' docs/milestones
rg -n '待统一|旧版|V1–V[0-9]' docs README.md
```

### 2.8 产品决策已经实现，但决策清单没有回填

**现象：** 运动消耗可手动修正、用户目标采用手动设置等规则已经进入代码和 UI，`DEVELOPMENT.md` 仍标为未确认，后续开发者可能再次做出冲突实现。

**固定规则：** 产品规则一旦进入已合并代码，立即回填决策清单、PRD 或 ADR，并写明采用的口径；聊天结论不能作为唯一依据。

### 2.9 统计口径必须集中定义

**现象：** 旧统计页只统计饮食，新趋势页需要同时处理摄入、运动、净摄入、完整日期、目标达成率、前一周期和时区。若前后端各自推断，会出现首页与趋势对不上。

**固定规则：**

- 后端是统计口径唯一来源；前端只格式化和绘图。
- 饮食统计读取记录快照，运动统计读取保存后的最终消耗值。
- 日期边界使用配置时区和可注入 `Clock`，测试不得依赖运行机器的当天日期。
- 每个聚合接口至少有一条与明细或首页的对账测试。

### 2.10 本地数据库备份必须持续隔离

**现象：** 历史 MySQL 迁移会在 `backend/var/backups/` 生成真实数据库备份。当前目录已被 `.gitignore` 正确忽略，但其中可能包含用户数据，不能因为 Git 未跟踪就随意上传或附到 PR。

**固定规则：**

- `backend/var/` 永久保持忽略，不执行强制添加。
- 共享迁移证据只使用脱敏后的行数、校验和和日志摘要，不共享 SQL dump。
- 清理备份前先确认恢复窗口和数据保留要求，不自动删除。

### 2.11 迁移测试的期望版本会随新迁移漂移

**现象：** M7 新增 `V7__exercise_records.sql` 后，本地默认测试因跳过外部 MySQL 用例而全部通过；CI 真实执行 MySQL 迁移测试时仍断言最新版本为 V6，导致业务代码无误但流水线失败。

**固定规则：**

- 新增每个 Flyway 迁移时，同时检查 `MySqlMigrationTest` 的最新版本断言、迁移数量和关键表断言。
- 本地“跳过外部 MySQL 用例”必须在交付说明中显式标出，不能等同于真实 MySQL 已通过。
- CI 失败先读取具体失败用例和环境变量，再判断是业务回归、迁移契约漂移还是运行时差异；不通过重跑或放宽检查掩盖问题。

## 3. M8 闭环退出条件

M8 已完成，退出条件和证据如下：

- [x] 后端统一趋势接口、完整日期、快照聚合、时区和规则总结完成。
- [x] 小程序 7/30/90 天切换、过期响应保护、无障碍摘要和状态处理完成。
- [x] 后端与小程序全量自动化通过。
- [x] 微信开发者工具普通编译问题面板为 0。
- [x] M7 先创建 PR、CI 通过、完成视觉验收并合并，创建 `m7-complete` 标签。
- [x] M8 整理到最新 `master`，确认 PR 只包含 M8 提交。
- [x] 使用契约兼容的本地服务完成 M8 成功态、少于 3 天态和 30/90 天横向图表联调。
- [x] 对照 `design/优化版/04-热量与运动趋势.png` 保存成功态截图并更新 `design-qa.md`。
- [x] 推送 M8、创建 PR、CI 全绿、合并后创建并推送 `m8-complete` 标签。

## 4. 推荐的下一步顺序

1. **开始 M9 前先更新主线。** 从包含 `m8-complete` 的 `master` 开新阶段分支，不在旧 M8 或后台管理工作树上叠加。
2. **先定义月历聚合契约。** 明确整月摘要接口、月份时区边界、缺失日期和一次加载策略，不复用旧“历史”页逐日请求模式。
3. **为聚合建立对账测试。** 月视图总量必须能与日趋势、首页和明细记录按相同口径对账。
4. **并行开发继续使用独立工作树。** 小程序里程碑与后台管理分别提交，提交前按路径检查暂存区。
5. **沿用四类完成证据。** 自动化、接口联调、开发者工具视觉验收、PR/CI/标签缺一不可。

## 5. 每次开工的最小检查

```bash
git status --short --branch
git fetch origin
git log --oneline --decorate -8
java -version
(cd backend && sh mvnw --batch-mode test)
node --test miniapp/tests/*.test.js
find miniapp -name '*.js' -not -path '*/miniprogram_npm/*' -print0 | xargs -0 -n1 node --check
git diff --check
```

随后在微信开发者工具执行普通编译并检查问题面板。涉及页面交付时，必须同时准备可复现的成功态数据和设计稿对照截图。

## 6. 管理后台必须吸收的经验

后台与小程序共用领域模型和统计口径，因此不能另起一套“后台习惯”。以下规则直接来自 M0–M8 已发生的问题。

### 6.1 先做可追踪矩阵，再做页面

**风险：** 仅根据后台原型开发，会自然增加公共食品审核、用户封禁、提醒规则和任务面板，但这些能力在当前小程序没有入口、模型或接口。

**固定规则：** 每个后台需求必须登记前台页面/入口、PRD 规则、数据来源、权限和开放里程碑。反查不到来源的功能标记为未来候选，不进入当前 Sprint。唯一映射表维护在 [`ADMIN-DEVELOPMENT.md`](./ADMIN-DEVELOPMENT.md#4-前台入口后台能力-11-映射)。

### 6.2 前台阶段是后台开放门

**风险：** 后台若提前绑定 M7/M8 本地分支或 M9/M10 设计字段，会再次出现堆叠分支、契约漂移和无法独立验收的问题。

**固定规则：**

- 食品后台只复用已经稳定的 M3–M5 能力。
- 运动、趋势、日历、目标和隐私能力分别等待 M7、M8、M9、M10 合并后开放。
- 可以提前完成界面骨架和 OpenAPI 评审，但不得用假接口把功能标记为完成。

### 6.3 领域服务是唯一口径，后台不重算

**风险：** 小程序、后台和报表各写一遍热量或统计公式，会让同一用户同一天出现三个结果。

**固定规则：** 后台 API 调用现有营养、首页、趋势和后续日历领域服务；前端只展示。所有跨端功能至少有一条同用户、同日期、同输入的契约对账测试。

### 6.4 先采集真实事件，再建设运营看板

**风险：** 当前业务表无法准确还原“进入记录页但未保存”“搜索返回空结果”或一次记录耗时。先画看板只会产生看似精确的假指标。

**固定规则：** 先评审事件字典和隐私字段，再实现采集、数据质量校验，最后开发 PRD 五项指标。每个指标必须显示定义、数据源、更新时间和异常状态。

### 6.5 后台写权限按历史一致性收紧

**风险：** 运营人员直接修改用户饮食、运动或目标，可能破坏营养快照、用户确认值和隐私预期。

**固定规则：**

- 首期只允许写系统食品、分类和管理员安全配置。
- 用户私有食品、饮食记录、运动记录、趋势和日历全部只读诊断。
- 规则使用版本发布和回滚，不直接编辑生效数据。
- 账号删除由小程序用户发起，后台只查看流程和处理证据。

### 6.6 后台也使用四类完成证据

后台功能的完成状态同样拆分为：

1. 自动化：单元、接口、权限、审计和跨端契约测试。
2. 联调：真实管理 API、真实权限和真实业务数据。
3. 视觉：常规、空、错、无权限、窄屏和键盘操作。
4. 交付：独立 PR、CI、合并和阶段记录。

原型可点击、页面能打开或假数据好看，都不能单独视为完成。

## 7. 提效后的双端开发循环

每个会影响前后台的功能采用以下顺序：

1. **产品契约：** 在 PRD/里程碑确认前台入口、业务口径、异常和管理需求。
2. **数据契约：** 确认领域模型、快照边界、读写权限和迁移；先写 OpenAPI 示例。
3. **后端领域：** 只实现一次计算或聚合逻辑，同时服务小程序 API 与管理 API。
4. **小程序闭环：** 完成正常、空、错、加载和重复提交状态。
5. **后台闭环：** 完成系统数据维护或只读诊断，不扩大业务边界。
6. **跨端对账：** 使用同一用户、日期和测试数据核对首页、明细、趋势、日历与后台。
7. **四类验收：** 自动化、联调、视觉和交付分别留证。
8. **文档回填：** 同步 PRD、API、架构、里程碑、后台映射和复盘。

推荐每个跨端需求在任务卡中固定包含：

```text
前台入口：
后台用途：
领域服务：
接口与示例：
数据读写边界：
开放里程碑：
自动化证据：
联调证据：
视觉证据：
PR / 标签：
```

这样可以在开工前发现范围漂移、接口猜测和阶段依赖，而不是等联调或提交时返工。
