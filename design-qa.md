# M8 热量与运动趋势视觉验收

- 设计基准：`design/优化版/04-热量与运动趋势.png`（390 × 844）
- 开发者工具截图：`design/qa/m8-trends-devtools-full.jpeg`
- 验收环境：微信开发者工具 Nightly 2.02.2607142，iPhone 14 Pro Max 模拟器
- 数据环境：本地只读视觉契约服务，与 `TrendResponse` 字段和 7d/30d/90d 参数一致

## 对照范围

已对照页面标题、统计周期、7/30/90 天切换、日均净摄入、横向柱状趋势、平均摄入、平均运动、营养达成率、周期比较和最多两条规则总结。90 天状态正确展示 04.17–07.15 完整周期并支持横向滚动；30 天状态展示 06.16–07.15。

## 运行与状态检查

- 7/30/90 天：三个范围均通过真实请求切换，日期长度、汇总指标和无障碍摘要随响应更新。
- 少于 3 天：仅 2 天有完整记录时展示“数据不足，暂不生成趋势结论”和“暂无周期对比”，不输出误导性总结。
- 并发：`trend-flow.test.js` 覆盖快速切换时忽略过期响应。
- 编译：微信开发者工具问题面板为 0；柱高使用有限 CSS class，不含动态内联样式。

## 最终结果

passed

---

# M7 今日运动与推荐视觉验收

- 设计基准：`design/优化版/03-今日运动与推荐.png`（390 × 844）
- 开发者工具截图：`design/qa/m7-exercise-devtools-full.png`
- 验收环境：微信开发者工具 Nightly 2.02.2607142，iPhone 14 Pro Max 模拟器
- 数据环境：本地只读视觉契约服务，与 `ExerciseDayResponse`、`ExerciseResponse` 和 `ExerciseRecommendationResponse` 字段一致

## 对照范围

已对照日期与标题、今日运动消耗、总时长、已完成运动列表、添加入口、两项规则推荐、本周完成度和底部导航。成功态展示 2 项运动、45 分钟、274 千卡和 3/4 次周完成度；运动卡片与推荐卡片没有横向溢出或意外换行。

## 迭代记录

1. 开发者工具 Nightly 会把 WXML 动态内联宽度编译成非法 CSS，而 Node 语法检查无法发现该问题。
2. 将首页圆环、营养进度和运动周进度统一改为有限 CSS class，保留业务计算值用于文本和测试。
3. 普通编译后问题面板为 0，首页与运动页成功态都能从同一契约数据加载并对账。

## 运行与状态检查

- 成功态：运动列表、时长、消耗、两项推荐和周完成度完整渲染。
- 空/错/加载/删除确认：`exercise-flow.test.js` 覆盖页面状态和交互保护；断开服务时页面显示错误与重试，不伪装为空数据。
- 首页对账：首页显示完成 2 项、45 分钟、274 千卡，与运动页一致；运动不增加剩余可摄入。
- 编译：微信开发者工具问题面板为 0。

## 最终结果

passed

---

# M3 食品搜索与记录页视觉验收

- 设计基准：`design/优化版/05-食品搜索与记录.png`（390 × 844）
- 实现截图：`design/qa/m3-record-page.png`
- 开发者工具全景：`design/qa/m3-devtools-full.png`
- 验收环境：微信开发者工具 iPhone 模拟器，截取设备区域
- 验收状态：成功态；使用与后端 `FoodResponse` 一致的本地契约数据注入列表

## 对照范围

已对照顶部安全区与标题、搜索框、拍照留存与自定义食品快捷入口、常用/最近/收藏分段、食品列表、每 100g 营养基准、添加入口、最近组合和底部导航。截图中文本与图标可清晰辨认，因此无需额外局部放大图。

## 迭代记录

1. 第一轮发现 P1：页面标题进入刘海/状态栏区域。
2. 将顶部安全区补偿由 28rpx 调整为 72rpx，并重新在微信开发者工具编译和截图。
3. 第二轮未发现影响交付的 P0、P1 或 P2 视觉问题。设备外壳和项目真实 TabBar 与设计稿展示框的差异属于运行环境与应用骨架差异。

## 运行与状态检查

- 成功态：常用食品列表、营养基准和最近组合正常渲染。
- 加载/空/错误/重试：由公共 `state-view` 组件统一呈现，断开后端时已在开发者工具确认错误态。
- 交互：搜索使用 300ms 防抖和请求版本保护；自定义食品使用底部表单；拍照入口明确为 M3 留存占位；食品添加进入 M4 热量计算路由壳。
- 编译：微信开发者工具问题面板为 0；已消除本次页面的 WXSS 标签选择器警告。后端未启动时出现的连接拒绝不属于编译错误。

## 最终结果

passed

---

# M6 今日热量总览视觉验收

- 设计基准：`design/优化版/01-首页-热量总览.png`（390 × 844）
- 实现截图：`design/qa/m6-today-dashboard.png`
- 验收环境：微信开发者工具 Nightly 2.02.2607142，iPhone 14 Pro Max 模拟器
- 数据环境：本地只读视觉契约服务，与 `DashboardTodayResponse` 字段一致

## 对照范围

已对照日期与安全区、热量圆环、剩余/摄入/目标/运动消耗、三大营养素进度、记录与拍照操作、运动摘要、单条建议、餐次摘要和底部导航。信息层级、暖米白/深苔绿配色、卡片圆角和进度条与基准一致。

## 迭代记录

1. 首轮发现日历入口接近微信右上角胶囊，且有运动时建议行未显示。
2. 将日历操作下移到胶囊安全区之外，并统一空运动/有运动的建议行。
3. 最终截图无主要内容裁切、意外换行或横向溢出。

## 运行与状态检查

- 成功态：单次聚合请求驱动所有首页模块。
- 空/超额/失败态：小程序流程测试覆盖剩余归零、无运动和单模块失败保留其他内容。
- 交互：记录一餐进入记录 Tab，拍照与 M9 日历提供明确阶段占位，餐次摘要进入对应餐次详情。
- 截图上方的微信胶囊、下方的原生 TabBar 属于真实运行容器，应保留而非像素级重画。

## 最终结果

passed

---

# M4 热量计算页视觉验收

- 设计基准：`design/优化版/02-热量计算.png`（390 × 844）
- 开发者工具截图：`design/qa/m4-devtools-full.jpeg`
- 验收环境：微信开发者工具 Nightly 2.02.2607142，iPhone 14 Pro Max 模拟器
- 数据环境：本地只读视觉契约服务，鸡胸肉 165 千卡、蛋白质 31g、脂肪 3.6g、碳水 0g（每 100g）

## 对照范围

已对照自定义顶部导航、食品摘要、重量输入、加减按钮、100g/150g/200g/一份快捷值、热量结果、三大营养素、餐次、备注、阶段边界提示和底部确认动作。150g 成功显示 248 千卡、蛋白质 46.5g、脂肪 5.4g、碳水 0g；没有 `servingAmount` 时“一份”呈禁用态。

## 迭代记录

1. 第一轮发现原生按钮的默认自动外边距挤压重量控件，并使底部确认按钮按内容收缩。
2. 将加减与确认动作改为明确尺寸的交互容器，保留按压态和 JS 层防重复保护。
3. 第二轮控件完整显示、页面无横向溢出，视觉层级和间距与 `02-热量计算.png` 一致。

## 运行与状态检查

- 成功态：食品、实时营养、餐次和备注完整渲染。
- 加载/错误/重试：使用公共 `state-view`；自动化覆盖接口失败状态更新。
- 输入边界：空、0、负数、超过 10,000g 和多位小数均显示就地错误并阻止确认。
- 防重复：`submitting` 在服务端复核期间阻止第二次请求。
- 编译：微信开发者工具问题面板为 0。

## 最终结果

passed

---

# M9 饮食日历视觉 QA

- Source visual truth: `design/优化版/06-饮食日历.png`
- Implementation screenshot: `docs/evidence/m9/calendar-recorded-day.png`
- Combined comparison: `docs/evidence/m9/calendar-comparison.png`
- Viewport: 微信开发者工具 iPhone 14 Pro Max，截图归一化为 390 × 840
- State: 2026-07 成功态，7 月 13 日已选中，有饮食和运动记录

## Findings

最终对照没有可执行的 P0、P1 或 P2 差异。

- Fonts and typography: 小程序系统字体、标题字重、数字层级和小字辅助信息与设计稿等价，无截断或异常换行。
- Spacing and layout rhythm: 顶部安全区、月份导航、6 行日历、摘要卡和餐次卡的垂直比例与参考一致；圆角和卡片间隔使用现有 Token。
- Colors and visual tokens: 背景、表面、主绿、今日浅绿、边框和次级文本均沿用项目色彩系统，选中与记录标记的对比充足。
- Image quality and assets: 导航继续使用仓库内 Heroicons 资产，未使用占位图、表情或伪造的位图资产。
- Copy and content: 页面标题、月份、摄入、餐次、运动、剩余和详情入口与设计稿意图一致。验收数据中餐次数和进度百分比按真实后端计算，不强行复制设计稿的示例数字。

## Interaction and runtime evidence

- 普通编译通过，问题面板为 0。
- 已验证首页日历入口、选中有记录日期、月度标记、摘要更新和餐次明细列表。
- 开发者工具未报告 WXML、WXSS 或 JavaScript 问题；本地成功态使用临时 H2 数据验收，临时会话未保留在源码。

## Comparison history

1. First pass: P1 顶部自定义导航侵入刘海安全区，P2 日历与摘要卡垂直密度偏紧。
2. Fixes: 增加顶部安全区，调整标题与月份间距、日期行高和摘要卡内边距。
3. Post-fix evidence: `calendar-comparison.png` 显示标题、月历、摘要和餐次区域与参考图的层级及比例对齐，无剩余 P0/P1/P2。

聚焦区域比较不再单独拆分：390px 宽的合并对照中，标题、日历数字、指标和餐次文本均可清晰阅读。

## Follow-up polish

- P3: 当前月的“下一月”按钮按业务规则禁用，因此比设计稿示例更浅；保留现有语义。

final result: passed
